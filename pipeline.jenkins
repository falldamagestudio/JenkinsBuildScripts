
@Library("JenkinsBuildScripts@master") _

echo "pipeline.jenkins starts"
def failedTests = getFailedTests()
for (failedTest in failedTests) {
    echo "failed test: ${failedTest.name}, ${failedTest.url}"
}
